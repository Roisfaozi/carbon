# Task: Add purchasing import fields (order multiple, supplier MOQ, purchasing lead time)

Spawn subtasks to query the cache folder any time I need to learn something about the codebase. NEVER update the cache with plans or information about code that is not yet committed.

## Interpretation of the three requested fields
- **Supplier order multiple** → `supplierPart.orderMultiple` — column does NOT exist; add to schema + import + edge-function write.
- **Supplier MOQ** → `supplierPart.minimumOrderQuantity` — ALREADY an import field ("Minimum Order Quantity"). No change; confirm to user.
- **Purchasing lead time** → `itemReplenishment.purchasingLeadTime` (item-level "Purchasing" tab). New import field + new edge-function UPDATE path (itemReplenishment is auto-created by `create_item_related_records` AFTER INSERT trigger, default 0; we UPDATE it post-insert).

## Steps
- [ ] Migration `20260526160000_supplier-part-order-multiple.sql`: `ALTER TABLE "supplierPart" ADD COLUMN IF NOT EXISTS "orderMultiple" INTEGER DEFAULT 1;` (mirrors nullable `minimumOrderQuantity`).
- [ ] `imports.models.ts`: add `orderMultiple` to `supplierPartImportFields`; add `itemPurchasingImportFields` const with `leadTime`; spread into part/tool/fixture/consumable/material; add `orderMultiple` + `leadTime` to those 5 `importSchemas` Zod objects.
- [ ] `import-csv/index.ts`:
  - `SupplierPartImportLink` + `supplierPartForInserts` type: add `orderMultiple?`.
  - both `supplierPartLinks.push` sites + the inserts push: carry `orderMultiple`.
  - `writeSupplierPartLinks`: parse + write `orderMultiple` (null when absent, like MOQ).
  - new `writeItemPurchasingLeadTimes` helper (UPDATE itemReplenishment); collect entries for inserts (parallel array) + updates; call after item upserts.
- [ ] Spread `...itemPurchasingImportFields` only on the 5 real item types (NOT methodMaterial / workCenter / etc).

## Notes / decisions
- Column name `orderMultiple` matches existing `itemPlanning.orderMultiple` convention.
- NOT touching SupplierPartForm/validator (explicit scope = schema + import). `sanitize()` on update protects imported value from being nulled by a later form save. Form field is a 1-line follow-up if wanted.
- types.ts cannot be regenerated by me (DB rebuild is the user's job) — typecheck will show transient errors on `supplierPart.orderMultiple` until the user rebuilds + regenerates types. Flag this.

## Review
- [x] Migration `20260526160000_supplier-part-order-multiple.sql` — adds nullable `orderMultiple INTEGER DEFAULT 1`.
- [x] `imports.models.ts` — `orderMultiple` in supplierPartImportFields; new `itemPurchasingImportFields` (`leadTime`) spread into the 5 item types (NOT methodMaterial); `orderMultiple` + `leadTime` added to the 5 item importSchemas. Verified 5 literal spreads, 5 schema additions each.
- [x] `import-csv/index.ts` — `SupplierPartImportLink.orderMultiple`, `ItemPurchasingLeadTime` type, `writeItemPurchasingLeadTimes` helper (UPDATE itemReplenishment), orderMultiple written in writeSupplierPartLinks (set+values), lead time collected for inserts (parallel array) + updates, both helpers called in txn.
- [x] erp typecheck: clean (exit 0). database tsgo: clean (edge fn is Deno, out of tsgo scope).
- **Supplier MOQ already existed** as import field "Minimum Order Quantity" — no change made.
- **Pending (user action):** rebuild DB + regenerate types so `supplierPart.orderMultiple` lands in the generated `Database` type. Until then the Deno typecheck of import-csv will flag `orderMultiple`. NEVER rebuild DB myself.
- Did NOT add `orderMultiple` to SupplierPartForm/validator (explicit scope was schema + import; `sanitize()` protects the imported value from form saves). 1-line follow-up if visibility wanted.
